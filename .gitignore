# ──── Plugins Obsidian (binários — baixados pelo setup.sh) ─────────────────────
vault/.obsidian/plugins/*/main.js
vault/.obsidian/plugins/*/styles.css

# ──── Obsidian workspace (específico de cada máquina) ──────────────────────────
vault/.obsidian/workspace.json
vault/.obsidian/workspace-mobile.json
vault/.obsidian/cache
vault/.obsidian/.trash/

# ──── Claude Code (sessões e projetos locais) ───────────────────────────────────
vault/.claude/projects/
vault/.claude/todos/

# ──── opencode (sessões) ────────────────────────────────────────────────────────
vault/.opencode/sessions/

# ──── Segredos e configuração local ─────────────────────────────────────────────
.env
.env.*
!.env.example
*.local
*.local.*
*secret*
*secrets*
*credential*
*credentials*
*token*
*.pem
*.key
*.p12
*.pfx
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
vault/**/.env
vault/**/.env.*
vault/**/*secret*
vault/**/*credential*
vault/**/*token*
vault/**/*.pem
vault/**/*.key

# ──── Dumps / backups locais ───────────────────────────────────────────────────
*.bak
*.backup
*.dump
*.sql
*.sqlite
*.sqlite3

# ──── VS Code ────────────────────────────────────────────────────────────────────
vault/.vscode/settings.json

# ──── OS ─────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
